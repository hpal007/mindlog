# Secrets — never commit
.env
.env.local
.env*.local

# Dependencies / build — keep repo < 10 MB
node_modules/
.next/
out/
dist/
build/
coverage/
playwright-report/
test-results/

# OS / editor
.DS_Store
*.log
npm-debug.log*
.vercel

# TypeScript build artifacts
*.tsbuildinfo
next-env.d.ts

# Local AI playbook + editor config (not product)
CLAUDE.md
.claude/
.gstack/
